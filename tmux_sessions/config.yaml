session_name: config
windows:
  - window_name: dev window
    layout: main-vertical 
    shell_command_before:
      - cd ~/dotfiles/ # run as a first command in all panes
    panes:
       - echo second pane # pane no. 1
       - shell_command: # pane no. 2
          - nvim
