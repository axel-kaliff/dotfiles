# Agent Delegation Rules

- Delegate to the `architect` agent before starting features with 3+ files
- Delegate to the `code-reviewer` agent after completing a feature
- Delegate to the `test-runner` agent after writing tests
- Delegate to the `code-reviewer` agent when touching files over 200 lines
